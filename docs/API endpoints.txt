GET /authorize - returns information about current session, user info and code 200 if user is logged in or 401 if user isn't logged
POST /authorize - containing information about users email and password provide functionallity to start session
DELETE /authorize - ends session of currently logged user
